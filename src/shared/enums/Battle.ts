export enum BattleStatus {
  Closed = 'closed',
  Active = 'active',
  Finished = 'finished',
}

export enum ActionType {
  Attack = 'attack',
  Speed = 'speed',
}

export enum RoundType {
  Initial = 'initial',
  OnGoing = 'ongoing',
  Closing = 'closing',
}
